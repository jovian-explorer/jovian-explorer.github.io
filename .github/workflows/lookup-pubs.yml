name: One-off publication lookup
on:
  push:
    branches: [testing]
    paths: [tools/lookup_pubs.py, .github/workflows/lookup-pubs.yml]
jobs:
  lookup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: python3 tools/lookup_pubs.py
