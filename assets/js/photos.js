/* Photograph list. Written by tools/photos.py; titles, descriptions, places
   and albums can be edited by hand and are kept on the next run.
   Fields: src, thumb, width, height, album, title, place, date (YYYY-MM),
   description, alt, cover (true = album cover), exif { camera, lens, focal,
   aperture, shutter, iso }. */
window.PHOTOS = [];
